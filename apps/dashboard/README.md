# @arcane/dashboard (placeholder)

**Lane C** — the Next.js + Supabase Realtime web dashboard that mirrors the terminal live
(Build Guide §6, milestone C1). Intentionally **empty in Session 0**.

It will be scaffolded with `create-next-app` and wired to the same `ResultEvent` stream the CLI
renders once the cloud engine emits one (E1). Until then this is just a private workspace member so
`npm install` resolves cleanly.
